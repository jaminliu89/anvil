schema_version: 2
product: Anvil
product_status: FAILED_NOT_USABLE
acceptance_gate: kim_test

capabilities:
  ANVIL-CAP-001:
    name: Desktop clean launch
    status: IMPLEMENTED
    claim: "Tauri shell no longer auto-starts DSH/model sidecar during boot"
    verification_evidence:
      - "src-tauri/src/lib.rs removes dsh::manager::init from setup"
    acceptance_evidence: []
    protected: false

  ANVIL-CAP-002:
    name: Unified timeline
    status: IMPLEMENTED
    claim: "Legacy timeline implementation exists but is not the default product entry"
    acceptance_evidence: []
    protected: false

  ANVIL-CAP-003:
    name: Local model chat
    status: IMPLEMENTED
    claim: "Legacy optional integration exists; it is not required for VS-001 startup"
    acceptance_evidence: []
    protected: false

  ANVIL-CAP-004:
    name: Coding worker execution boundary
    status: IMPLEMENTED
    claim: "Owned CodingWorker boundary now creates Dock sessions, waits for real plan state, and performs truthful approval transition; execution events/evidence are still pending"
    verification_evidence:
      - "src/runtime/coding-worker.ts owns Task -> Dock planning/approval boundary"
      - "src/views/TaskView.vue calls CodingWorker rather than slash-command adapter path"
      - "UI moves to executing only after backend approval returns success"
    acceptance_evidence: []
    protected: false

  ANVIL-CAP-005:
    name: Dock asynchronous dispatch
    status: IMPLEMENTED
    claim: "VS-001 Task -> CodingWorker -> Dock plan/approval path is implemented but not machine-verified on founder environment"
    verification_evidence:
      - "Dock health check precedes session creation"
      - "Dock session + plan are surfaced through owned CodingWorker contract"
    acceptance_evidence: []
    protected: false

  ANVIL-CAP-006:
    name: Training integration
    status: IMPLEMENTED
    claim: "Legacy integration exists outside the VS-001 critical path"
    acceptance_evidence: []
    protected: false

  ANVIL-CAP-007:
    name: Harness integration
    status: IMPLEMENTED
    claim: "Legacy optional integration exists outside the VS-001 critical path"
    acceptance_evidence: []
    protected: false

  ANVIL-CAP-008:
    name: Founder core vertical slice
    status: IMPLEMENTED
    claim: "Clean boot + Repository/Task/Run + real CodingWorker planning/approval path are implemented; execution events, tests/diff, accept/reject, machine verification and founder acceptance remain pending"
    verification_evidence:
      - "src/router/index.ts defaults / to TaskView"
      - "src/views/TaskView.vue provides repository picker, task input and Run without slash commands"
      - "src/App.vue removes DSH/model status and legacy navigation from default shell"
      - "src-tauri/src/lib.rs does not auto-start DSH sidecar"
      - "src/runtime/coding-worker.ts creates real Dock sessions and waits for plans"
    acceptance_evidence: []
    protected: false

claim_policy:
  public_support_claim_requires: ACCEPTED
  accepted_capabilities: []
  current_product_claim: "Architecture experiment under rescue; VS-001 planning/approval path implemented but not verified/accepted"
